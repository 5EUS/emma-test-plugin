{
  "name": "video-runtime-smoke",
  "displayName": "Video Runtime Smoke",
  "description": "Probes the video transport path for the active runtime by requesting stream metadata for a configured media id. This validates scenario/runtime wiring even while the sample plugin still returns placeholder video data.",
  "defaultQuery": "demo-video-1",
  "queryLabel": "Video media id",
  "steps": [
    {
      "op": "set",
      "value": "$query",
      "save": "mediaId"
    },
    {
      "op": "videoStreams",
      "mediaId": "$mediaId",
      "save": "streams"
    },
    {
      "op": "log",
      "message": "Video stream probe for {{mediaId}} returned {{streams}}"
    }
  ]
}